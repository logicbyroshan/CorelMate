# Testing

The executable smoke test checks placeholders, replacement, quantity validation, page fitting, and multi-page planning and passes with .NET 10. Real v27.1.0.129 temporary-document tests verified grouped generation, 100 badges across 5 pages in about 9 seconds, missing-value rejection before mutation, and shape-count undo/redo: 1 before, 4 after, 1 after undo, 4 after redo. The final clean release build passed, the ZIP contained eight intended files, and extracted package install/uninstall passed. The hardened WPF Docker loaded through `AddDocker`, but foreground screenshot automation selected another application, so button-driven UI interaction and visual verification remain unverified. Formatting variants, 500/1000 quantities, and document switching remain unverified.

Task 6 real-host checks on v27.1.0.129 verified artistic text inside a group converts to `cdrCurveShape`, a rectangle remains `cdrRectangleShape`, Undo restores `cdrTextShape`, and Redo restores the curve. Paragraph text converted in a temporary document. A selected set containing visible, locked, and hidden text reported 3 found, 1 convertible, 1 locked, and 1 hidden. Special containers, symbols, text-on-path, and the WPF confirmation click path remain unverified.