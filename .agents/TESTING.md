# Testing

The current executable smoke test checks a pure badge grid calculation and passes with .NET 10 without CorelDRAW. The solution also builds successfully against the locally installed CorelDRAW interop assemblies. On 2026-09-16, `Build-Release.ps1` passed clean restore/build/test/package validation. The generated ZIP was extracted to a temporary directory; its installer made the Docker visible in CorelDRAW 27.1.0.129 and its uninstaller removed it. Automatic startup packaging, fresh-launch persistence, duplicate detection across restarts, and GUI assertions across a fresh user profile remain unverified.